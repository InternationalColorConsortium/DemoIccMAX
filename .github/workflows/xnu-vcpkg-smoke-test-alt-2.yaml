###############################################################
#
## Copyright (©) 2024-2025 David H Hoyt. All rights reserved.
##                 https://srd.cx
##
## Last Updated: 07-MAR-2025 at 1130 EST by David Hoyt (©)
#
## Intent: vcpkg ports file XNU smoke test
#
## TODO: 
#
#
###############################################################

name: "VCPKG XALT Smoke Test"

on:
  workflow_dispatch:

jobs:
  build-linux:
    name: VCPKG XNU Smoke Test
    runs-on: macos-latest
    strategy:
      fail-fast: false

    steps:
    # Checkout repository
    - name: Checkout repository
      uses: actions/checkout@v4

    # Set up Git anonymous identity
    - name: Set up Git anonymous identity
      run: |
        git config --global user.email "github-actions@github.com"
        git config --global user.name "GitHub Actions"
    # Clone DemoIccMAX repository and checkout vcpkg branch
    - name: Clone DemoIccMAX repository
      run: |
        git clone https://github.com/InternationalColorConsortium/DemoIccMAX.git
        cd DemoIccMAX
        git checkout vcpkg
        rm -f vcpkg.json
        pwd
        cd ..
        git clone https://github.com/microsoft/vcpkg.git
        cd vcpkg
        ./bootstrap-vcpkg.sh
        ./vcpkg integrate install
        pwd
        ls ../
        cd ../DemoIccMAX
        pwd
        ls
        ../vcpkg/vcpkg --classic install iccdev --overlay-ports=ports
        ../vcpkg/vcpkg list
